// Streeem Feature Flags
// Toggle features on/off without code changes.
// To disable a feature: set its flag to false and deploy.

export const FEATURE_FLAGS = {
    GAME_ENABLED: true,
} as const;
